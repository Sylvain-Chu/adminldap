# Example environment variables for LDAP and app behavior
# If you want the app to actually connect to an LDAP server, set the LDAP_* vars below.

# Admin authentication password (for /admin routes)
ADMIN_PASSWORD=admin123

# LDAP connection (optional). If not set, the app will write LDIF files to `data/ldif/` instead of adding to LDAP.
LDAP_URL=ldap://127.0.0.1:389
LDAP_BIND_DN=cn=admin,dc=homelab,dc=churlet,dc=eu
LDAP_BIND_PW=changeme
LDAP_BASE_DN=dc=homelab,dc=churlet,dc=eu

# Where to create home directories when generating LDIF entries
HOMEDIR_BASE=/mnt/pool/users

# Default unix group id for new users (posixAccount gidNumber)
DEFAULT_GID=3000

# Start value for uidNumber allocation when generating new users
UIDNUMBER_START=3000

# Start value for gidNumber allocation when creating new groups
GIDNUMBER_START=3000

# Default shell for created accounts
DEFAULT_SHELL=/usr/sbin/nologin
